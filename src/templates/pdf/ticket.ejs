<!DOCTYPE html>
<html lang="en">
  <head>
  <title>Ticket</title>

    <style>
      .cardWrap {
        width: 100%;
        margin: auto;
        color: #fff;
        font-family: sans-serif;
        display: -webkit-box;
        display: -ms-flexbox;
        display: flex;
      }
      .card {
        background: linear-gradient(
          to bottom,
          #5f2eea 0%,
          #5f2eea 26%,
          #ecedef 26%,
          #ecedef 100%
        );
        height: 15em;
        display: -webkit-box;
        display: -ms-flexbox;
        display: flex;
        -webkit-box-orient: vertical;
        -webkit-box-direction: normal;
        -ms-flex-direction: column;
        flex-direction: column;
        position: relative;
        padding: 1em;
        margin-top: 100px;
      }
      .cardLeft {
        border-top-left-radius: 8px;
        border-bottom-left-radius: 8px;
        width: 90%;
      }
      .cardRight {
        border-top-right-radius: 8px;
        border-bottom-right-radius: 8px;
      }
      
      h1 {
        font-size: 1.1em;
        margin-top: 0;
      }
      h1 span {
        font-weight: normal;
      }
      .title,
      .name,
      .date,
      .seat,
      .ticket,
      .time,
      .price {
        text-transform: uppercase;
        font-weight: normal;
      }
      .title h2,
      .name h2,
      .date h2,
      .ticket h2,
      .seat h2,
      .time h2,
      .price h2 {
        font-size: 0.9em;
        color: #525252;
        margin: 0;
      }
      .title span,
      .name span,
      .date span,
      .ticket span,
      .seat span,
      .time span,
      .price span {
        font-size: 0.7em;
        color: #a2aeae;
      }
      .title {
        margin: 2em 0 0 0;
      }
      .name,
      .date,
      .ticket,
      .seat,
      .price {
        margin: 0.7em 0 0 0;
      }
      .time,
      .ticket,
      .price {
        margin: 0.7em 0 0 1em;
      }
      .flex {
        display: -webkit-box;
        display: -ms-flexbox;
        display: flex;
      }
    </style>
  </head>

  <body>
    <div class="cardWrap">
      <div class="card cardLeft cardRight">
        <h1>Tickitz</h1>
        <div class="title">
          <span>movie</span>
          <h2><%= newDataBookingTicket.name %></h2>
        </div>

        <div class="name">
          <span>name</span>
          <h2><%= newDataBookingTicket.premiere %></h2>
        </div>

        <div class="flex">
          <div class="date">
            <span>date</span>
            <h2><%= newDataBookingTicket.dateBooking %></h2>
          </div>
          <div class="time">
            <span>time</span>
            <h2> <%= newDataBookingTicket.timeBooking %> </h2>
          </div>
          <div class="ticket">
            <span>ticket</span>
            <h2><%= newDataBookingTicket.totalTicket %> </h2>
          </div>
        </div>

        <div class="flex">
          <div class="seat">
            <span>seats</span>
            <h2><%= newDataBookingTicket.seat %></h2>
          </div>
          <div class="price">
            <span>price</span>
            <h2><%= newDataBookingTicket.totalPayment %> </h2>
          </div>
        </div>
      </div>
  </body>
</html>